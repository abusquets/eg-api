apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: events-guard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: docker.io/${DOCKER_USERNAME}/eg-api:${GITHUB_SHA}
        command: ["/prod/go-events-guard", "server"]
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health 
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
          successThreshold: 1
